job.raddes.de {
    @api path /jobs /jobs/* /jira/* /auth/* /user/* /admin/*
    handle @api {
        reverse_proxy api:5500
    }

    handle {
        root * /usr/share/caddy
        file_server
    }
}
